rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // HMS app: require sign-in. Tighten for production (admin claims, field-level checks).
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
